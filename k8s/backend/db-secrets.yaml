# db-credentials.yaml
# This file is a placeholder template for Kubernetes Secret.
# Actual credentials are injected securely at deploy time via GitHub Actions.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: nycad-app
type: Opaque
stringData: {}
