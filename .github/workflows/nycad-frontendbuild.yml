name: Frontend Build, Test & Push to ECR

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
    paths:
      - 'frontend/**'
      - '.github/workflows/nycad-frontendbuild.yml'
  workflow_dispatch:
 

env:
  AWS_REGION: us-east-1
  AWS_ID_ACCOUNT: ${{ secrets.AWS_ACCOUNT_ID }}
  FRONTEND_REPO: nycad-frontend-repo
  EKS_CLUSTER: ${{ secrets.EKS_CLUSTER }}
  K8S_NAMESPACE: nycad-app
  k8S_MANIFEST_DIR: k8s

jobs:
  build-test-push-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        with:
          projectBaseDir: frontend
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run npm audit
        working-directory: frontend
        run: npm audit --audit-level=high

      - name: Run frontend lint
        working-directory: frontend
        run: npm run lint --if-present

      - name: Run frontend tests
        working-directory: frontend
        run: npm test --if-present

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.$AWS_REGION.amazonaws.com

      - name: Build frontend Docker image
        run: |
          docker build -t $FRONTEND_REPO ./frontend
          docker tag $FRONTEND_REPO:latest ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com/$FRONTEND_REPO:latest

      - name: Run Trivy vulnerability scan on image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.FRONTEND_REPO }}:latest'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
          
      - name: Push frontend Docker image to ECR
        run: |
          docker push ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com/$FRONTEND_REPO:latest

      - name: Update kubeconfig
        run: aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ secrets.EKS_CLUSTER }}

      - name: Install kubectl (correct arch)
        run: |
          ARCH=$(uname -m)
          if [ "$ARCH" = "x86_64" ]; then ARCH="amd64"; fi
          curl -LO "https://s3.us-west-2.amazonaws.com/amazon-eks/1.33.5/2025-09-19/bin/linux/${ARCH}/kubectl"
          chmod +x ./kubectl
          sudo mv ./kubectl /usr/local/bin/kubectl
          kubectl version --client

      - name: Replace placeholders in manifests
        run: |
          sed -i "s|\${AWS_ACCOUNT_ID}|${AWS_ACCOUNT_ID}|g" k8s/frontend/frontend-deployment.yaml
          sed -i "s|\${AWS_REGION}|${AWS_REGION}|g" k8s/frontend/frontend-deployment.yaml

      - name: Apply namespace and manifests
        run: |
          kubectl create namespace nycad-app --dry-run=client -o yaml | kubectl apply -f -
          kubectl apply -f k8s/frontend/frontend-deployment.yaml -n nycad-app
          kubectl apply -f k8s/frontend/frontend-service.yaml -n nycad-app
          kubectl apply -f k8s/frontend/ingress.yaml -n nycad-app

      - name: Verify rollout
        run: |
          set -e
          kubectl delete rs -n nycad-app --all
          kubectl rollout restart deployment frontend-deployment -n nycad-app
          kubectl -n nycad-app rollout status deployment/frontend-deployment --timeout=1200s || (
            kubectl -n nycad-app get pods -l app=frontend -o wide
            kubectl -n nycad-app describe deployment frontend-deployment
            kubectl -n nycad-app get events --sort-by=.metadata.creationTimestamp | tail -n 20
            exit 1
          )
